add(cleanup('ALT-SPACEBAR|Activate the system menu to get to the About Box')
add(cleanup('ESCape|exit Fullscreen mode, or exit the app if in window mode')
add(cleanup('SPACEBAR |pause/resume playback (also left mouse double-click on the video, or right mouse single-click)')
add(cleanup('Ctrl-Up Arrow |increase playback speed 10%')
add(cleanup('/|increase playback speed 10%')
add(cleanup('Ctrl-Down Arrow|decrease playback speed 10%')
add(cleanup('\|decrease playback speed 10%')
add(cleanup('F12|open media file in third party video editor (currently ShotCut)')
add(cleanup('=|copy media file name to clipboard')
add(cleanup('A|play the first media file in the playlist (Z plays last)')
add(cleanup('B|[B]lackout/restore progress [B]ar')
add(cleanup('C|show/Hide on-screen [C]ontrols and media file timestamp')
add(cleanup('Ctrl-C|show/Hide all on-screen controls, media file timestamp and media metadata')
add(cleanup('D and DEL|[D]elete current media file (after confirmation)')
add(cleanup('Ctrl-D and Ctrl-DEL|[D]elete all files in the current media file's folder (after confirmation)')
add(cleanup('E|[E]ars - Mute/Unmute sound')
add(cleanup('F|show/cancel [F]ullScreen mode')
add(cleanup('G|[G]reater window size')
add(cleanup('Ctrl-G|reduce, i.e. un[G]reater, the window size')
add(cleanup('H|position the window [H]orizontally (and Vertically) in the center of the screen')
add(cleanup('I|zoom [I]n by 10% of the video's height and width')
add(cleanup('J|ad[J]ust the window's aspect ratio to match the video's aspect ratio')
add(cleanup('K|mark this media file as [K]eep')
add(cleanup('L|re[L]oad the list of supported media files from the current folder')
add(cleanup('M|[M]aximize / restore window')
add(cleanup('N|mi[N]imize window to the Windows taskbar')
add(cleanup('O|zoom [O]ut by 10% of the video's height and width')
add(cleanup('P |pause the media file and play it instead with [P]otplayer, if installed')
add(cleanup('Q|play the previous media file in the [Q]ueue/playlist')
add(cleanup('R|[R]ename the current media file')
add(cleanup('S|re[S]tart the current media file from the beginning, aka [S]tartover')
add(cleanup('T|[T]ab through the media file a 200th, 100th (default), 50th, 20th or 10th of its duration (use SHIFT-ALT, ALT, SHIFT, CAPS LOCK to modify)')
add(cleanup('Ctrl-T|[T]ab back through the media file a 100th (default), 50th, 20th or 10th of its duration (use ALT, SHIFT, CAPS LOCK to modify)')
add(cleanup('TAB|tab forwards 1/200th the duration of the media file')
add(cleanup('Ctrl-TAB|tab backwards 1/200th the duration of the media file')
add(cleanup('U|[U]nzoom, i.e. re-fit the video to the window')
add(cleanup('V|maximize / restore [V]iew, same as [M]')
add(cleanup('W|[W]atch the next video in the list (or play the next audio)')
add(cleanup('X|e[X]it the application')
add(cleanup('Y|tr[Y]out the media file by sampling it at various stages')
add(cleanup('Z|play the last media file in the playlist (A plays the first)')
add(cleanup('Up Arrow|increase the volume by 1%')
add(cleanup('Down Arrow|decrease the volume by 1%')
add(cleanup('Ctrl-Up Arrow|increase the playback speed by 10%')
add(cleanup('Ctrl-Down Arrow|decrease the playback speed by 10%')
add(cleanup('0|briefly show media caption')
add(cleanup('1|reset the playback speed to normal, i.e. [1]00%')
add(cleanup('2|resize the window so that 2 instances of the application can be placed side-by-side')
add(cleanup('4|resize to a mini-window in the top-right corner of the screen')
add(cleanup('Ctrl-4|move to top-right corner of the screen but maintain current window size')
add(cleanup('5|save/bookmark the current media file timestamp to an INI file')
add(cleanup('6|retrieve a saved/bookmarked media file timestamp from an INI file and continue playback from that point')
add(cleanup('7|delete any previously saved/bookmarked media file timestamp')
add(cleanup('8|set the video to 1-pixel larger than the window on all four sides')
add(cleanup('9|resize the window to the width of the video')
add(cleanup('RIGHT ARROW|step forwards one frame')
add(cleanup('LEFT ARROW|step backwards one frame')
add(cleanup('Zoom|')
add(cleanup('Ctrl-RIGHT ARROW|when zoomed in/out, move video RIGHT inside the window')
add(cleanup('Ctrl-LEFT ARROW|when zoomed in/out, move video LEFT inside the window')
add(cleanup('Ctrl-UP ARROW|when zoomed in/out, move video UP inside the window')
add(cleanup('Ctrl-DOWN ARROW|when zoomed in/out, move video DOWN inside the window')
add(cleanup('Additional |')
add(cleanup('Left Click on the window background and hold|the window can be dragged/repositioned without a window caption title bar')
add(cleanup('Left double-click video |pause / resume playback')
add(cleanup('Right single-click video |pause / resume playback')